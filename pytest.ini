[pytest]
# Raíz del repo en el path para que `import pipeline` y `import backend` funcionen
# al correr `pytest` desde la raíz, sin necesidad de instalar el proyecto.
pythonpath = .
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q
